import ParentManagement from "./ParentManagement";

export const metadata = {
  title: "Parent Management - ENG Dashboard",
  description: "Manage registered parent account owners and their child players.",
};

const ParentManagementPage = () => {
  return <ParentManagement />;
};

export default ParentManagementPage;
